# Core: TimestampSplit + the val_set->fit() wiring that BPR early stopping
# relies on both shipped in cornac 2.4.0. This project only uses cornac's
# public API, so a plain pip install is enough — no cornac source checkout.
cornac>=2.4.0

numpy>=1.23
scipy>=1.9
torch>=2.0
matplotlib>=3.6

# LightGCN only: cornac's LightGCN backend imports dgl lazily. Install if you
# run the LightGCN model/runner; not needed for BPR or NeuMF.
# dgl>=1.1

# Self-tests only: research/smoke/ runs standalone via `python -m` with nothing
# extra. pytest is needed solely to run the same checks as `pytest research/smoke`.
# pytest>=7
